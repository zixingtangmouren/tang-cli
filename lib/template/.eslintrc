{
  "parser": "babel-eslint",
  "extends": "airbnb",
  "env": {
    "browser": true,
    "node": true
  },
  "rules": {
    "semi": ["error", "never"],
    <% Object.keys(rules).forEach((key) => { %>
    "<%= key %>":"<%= rules[key] %>",
    <% })%>
  }
}
